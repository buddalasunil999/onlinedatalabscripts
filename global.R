require(jsonlite)
require(xtable)

library(SparseM)

#Bivariate analysis categorical ~ categorical
library(quantreg)
library(foreign)
library(nnet)
library(ggplot2)
library(reshape2)

library(splines)
library(survival)

library(pROC)
